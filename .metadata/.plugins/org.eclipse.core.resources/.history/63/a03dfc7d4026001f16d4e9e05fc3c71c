package com.mysite.sbb.comment;

public class Mapper {
	public Comment toDto(Comment comment) {
		String content = comment.getContent();
		String createDate = comment.getCreateDate().toString();
		String author = comment.getAuthor().toString();
		return new CommentDTO(content, createDate, author);
	}
}
